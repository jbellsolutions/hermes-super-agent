component: runtime.aider
type: specialist-runtime
description: "Git-aware incremental coding via Aider CLI. Wraps vendor/aider."
depends_on:
  agent-os.orchestrator: ">=0.1"
agents: []
data_sources: []
outputs:
  - type: vault_run_artifact
    consumer: vault.runs
