component: runtime.browser_use
type: specialist-runtime
description: "Structured browser automation. Wraps vendor/browser-use."
depends_on:
  agent-os.orchestrator: ">=0.1"
agents: []
data_sources: []
outputs:
  - type: vault_run_artifact
    consumer: vault.runs
