component: runtime.e2b
type: specialist-runtime
description: "Sandboxed code execution via E2B SDK — clean VM per run."
depends_on:
  agent-os.orchestrator: ">=0.1"
agents: []
data_sources: []
outputs:
  - type: vault_run_artifact
    consumer: vault.runs
