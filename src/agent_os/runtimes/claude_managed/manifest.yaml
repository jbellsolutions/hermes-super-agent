component: runtime.claude_managed
type: specialist-runtime
description: "Long-running cloud jobs via Anthropic Claude Managed Agents API."
depends_on:
  agent-os.orchestrator: ">=0.1"
agents: []
data_sources: []
outputs:
  - type: vault_run_artifact
    consumer: vault.runs
