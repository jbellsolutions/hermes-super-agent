component: runtime.codex_cli
type: specialist-runtime
description: "Background coding via OpenAI Codex CLI subprocess. Multi-provider hedge."
depends_on:
  agent-os.orchestrator: ">=0.1"
agents: []
data_sources: []
outputs:
  - type: vault_run_artifact
    consumer: vault.runs
