"""Stream: mcp_registry — discover new MCP servers, offer to Hermes."""
from __future__ import annotations


def run() -> dict:
    """TODO(stage-5): scan ~/.claude/mcp.json + remote registries for new servers."""
    return {"status": "stub", "todo": "wire MCP registry walk"}
