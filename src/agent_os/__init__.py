"""agent-os — One agent. One state. Every channel.

Hermes (orchestrator) + specialist runtimes + AGI-1 (quality) + Vault + Manifest +
Upgrader + Channels — all wired together with daily auto-updates.
"""

__version__ = "0.1.0"
