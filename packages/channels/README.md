# channels (source: `src/agent_os/channels/`)

> The Python source for this package lives at [`../../src/agent_os/channels/`](../../src/agent_os/channels/) and is installed as part of the top-level `agent-os` distribution.
>
> This directory is preserved for reference and per-package documentation.

See [`ARCHITECTURE.md`](../../ARCHITECTURE.md) for how this package fits into the system.
