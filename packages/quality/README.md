# quality (source: `src/agent_os/quality/`)

> The Python source for this package lives at [`../../src/agent_os/quality/`](../../src/agent_os/quality/) and is installed as part of the top-level `agent-os` distribution.
>
> This directory is preserved for reference and per-package documentation.

See [`ARCHITECTURE.md`](../../ARCHITECTURE.md) for how this package fits into the system.
